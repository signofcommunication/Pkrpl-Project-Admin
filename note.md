What packages u need it

- Firebase
- MUI
- React-Router-Dom

Step by step building admin dashboard

1. Create the UI(Login,Admin Dashboard)
2. Create authentication functionality & private route(store private route value in storage not in state)
3. Apa aja isi dashboard

# Algorithm

1. Login [DONE]

- Create the container
- Create the paper
- Create the form

2. Create the routes [DONE]
3. Create the provider for the whole components [DONE]
4. Create the function for register and login [DONE]
5. Create the private route for dashboard page

- Store the "isAuthenticated" value into storage so the data is immortal

# Backend

Features

- Authentication & Authorization API using JWT
- Filter functionality

Packages

- jsonwebtoken
- mongoose
- dotenv
- cors
- bcryptjs
- helmet(security)
- http-status-codes

# Algorithm

1. Setup the folder structure
2. Setup the basic server
3. Prepare the mongodb
4. Create function to connect the mongodb and define the schema then run the function
5. Define the schema
6. Create the function for CRUD in controller folder
7. Create the routes and define each routes
8. Create the function for post and get all jobs route first
9. Test the post route by upload from client side
   - Intsall axios,react-multiple-image-uploader
   - Create the minimal UI then to upload to db
10. Create the ui of the dashboard

- Add Navbar
- Add route for upload the content
- 
